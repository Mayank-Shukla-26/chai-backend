// Store the MongoDB database name in a constant so it can be reused across the project.
// This helps avoid hard-coding the string in multiple places and makes it easier to change later.
export const DB_NAME = "videotube"